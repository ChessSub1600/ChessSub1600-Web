// services/emailService.js

// Ya no hay lógica de envío de correos.
// Si no lo vas a usar, puedes borrar este archivo.

async function enviarCorreoBienvenida(destinatario, nombreUsuario) {
  // función stub, no hace nada
}

module.exports = { enviarCorreoBienvenida };
